#include <stdio.h>
#include <stdlib.h>



int main()
{
    int userInput;
    while true
        printf("Insert 1 to quit\n");
        printf("Insert any other number to continue\n\n");
        scanf("(> %d", &userInput);
        if (userInput == 1)
        {
            return 0;
        }
        else {
            prinf("God says: \n")
        }

    return 0;
}
